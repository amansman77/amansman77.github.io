source "https://rubygems.org"

# GitHub Pages gem - includes Jekyll and all dependencies
# This ensures compatibility with GitHub Pages and automatic security updates
# github-pages already includes: jekyll, jekyll-feed, jekyll-sitemap, jekyll-seo-tag, minima, etc.
gem "github-pages", group: :jekyll_plugins
